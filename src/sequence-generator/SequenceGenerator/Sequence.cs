using System;

namespace SequenceGenerator
{
    public class Sequence
    {
        /*
            A simple string manipulation method which takes an input string and produce a
            sequence containing the same letters or numbers in the input string, based on
            the following criteria:
            -> Each character in the output would be repeated 'n' number of times, where n is the position on the character in the input string
            -> Each character sequence should have the first item capitalized, if it is an alphabet
            -> Each character sequence should be separated by a '-' character
        */
        public string Generate(string value)
        {
            throw new NotImplementedException();
        }
    }
}
